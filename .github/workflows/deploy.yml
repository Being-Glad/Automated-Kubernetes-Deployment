name: Ping

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Workflow triggered on $GITHUB_REF at $(date -u)"
